// Development orchestration script — implemented in Phase 2
// Runs backend + renderer + electron concurrently
console.log("Dev script placeholder — run individual packages with pnpm dev:backend / dev:renderer");
